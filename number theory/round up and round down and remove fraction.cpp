#include <math.h>
#include <iostream>
using namespace std ;
int main ()
{
	double num;
	cin>>num;
	cout<<"round up (ciel)"<<ceil(num)<<endl;
	
	cin>>num;
	cout<<"round down (floor)"<<floor(num)<<endl;
	
    cin>>num;
    cout<<"remove fraction (trunc)"<<trunc(num)<<endl;
	
	
}
